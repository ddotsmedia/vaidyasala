# DECISIONS — Vaidyasala

Autopilot decisions log (LAW 1). Format: `- [date] [choice] because [reason]`.
- [2026-07-15] Authored LAW 7 + docs/PHASES.md Appendix A myself because no vaidyasala-PHASES*.md existed in Downloads and the prompt pack had no Appendix A; extracted phase prompts from PART 4 of the prompt pack and defined RUN AUTOPILOT as a sequential, resumable phase loop stopping before the human-gated Phase 7.
- [2026-07-15] docs/ARCHITECTURE.md sourced from Downloads/vaidyasala-architecture_2.md (latest of 3 identical copies) because Phase 1A onward requires it and LAW 5 forbids later modification.
- [2026-07-15] Dev compose host ports set to 55432/56379/57700 (pg/redis/meili) because 6379 and likely 5432 were already allocated on the local machine; bound to 127.0.0.1 only.
- [2026-07-15] Workspace packages consumed as TypeScript source (exports point at ./src, web transpilePackages, worker via tsx/tsc) instead of a per-package build step because it removes build ordering complexity for an internal monorepo.
- [2026-07-15] ioredis pinned via pnpm-workspace overrides + BullMQ given plain ConnectionOptions (not a shared Redis instance) because bullmq bundled a second ioredis version causing type-identity conflicts.
- [2026-07-15] typedRoutes disabled until Phase 3B because the nav/footer reference public routes (/topics, /latest, ...) that do not exist until Phase 3; re-enable when those pages land.
- [2026-07-15] Fonts loaded via next/font/google (Inter latin, Anek Malayalam malayalam+latin, Noto Serif Malayalam malayalam preload=false) instead of self-hosted WOFF2 because next/font auto-subsets, preloads, and emits size-adjusted fallback metrics (zero CLS) per §5.3.
- [2026-07-15] Tier-1 primitives hand-written on Radix + cmdk + sonner (not shadcn CLI) because the CLI cannot scaffold non-interactively into a workspace package; styled with @theme tokens.
- [2026-07-15] AI model tiers: workhorse=claude-sonnet-5, cheap=claude-haiku-4-5 (per §8.1 Sonnet/Haiku split, mapped to current model IDs via the claude-api reference).
- [2026-07-15] Structured AI output uses a manual completeJson(Zod + 2-retry repair) loop with providers accepting injected clients/fetch, rather than output_config.format, so the whole chain is unit-testable with mocked providers and makes zero real API calls in CI.
- [2026-07-16] Media storage abstracted behind an S3-compatible StoragePort (@aws-sdk/client-s3); dev uses a scoped MinIO service in compose.dev.yml (vaidyasala-dev-minio, 127.0.0.1:59000) instead of BLOCKING on R2 creds, because Docker is available locally and it makes ingest media upload runnable now; prod swaps the endpoint to R2. Storage auto-creates the media bucket on first put.
- [2026-07-16] Ingest metadata: prefer YouTube Data API when YOUTUBE_API_KEY is present, else fall back to `yt-dlp -J` (no key required), so ingest can run without an API key. yt-dlp/ffmpeg also power audio extraction; absent binaries mark the audio step BLOCKED without sinking ingest.
- [2026-07-16] Queue contract (QUEUE_NAMES, PIPELINE_STAGES §8.2 order, idempotencyKey, parseYouTubeId, ingestInputSchema) lives in packages/core/queue (pure, no bullmq/ioredis) so apps/web (enqueue) and apps/worker (consume) share one contract without web importing worker (§3).
- [2026-07-16] Per-video pipeline is a BullMQ FlowProducer chain nested so the leaf (asr) runs first and completion bubbles to the root (quality-gate). Ingest enqueues it only once ≥1 stage processor is registered (none until 2C), so 2B ingest stops cleanly at PROCESSING instead of faking downstream success or parking DLQ noise.
- [2026-07-16] DLQ = dedicated `vaidyasala.dlq` queue; a Worker 'failed' listener copies terminally-failed jobs (attemptsMade ≥ attempts) there for admin visibility. Retries use exponential backoff ×5 (JOB_OPTS).
- [2026-07-16] .env.example was rewritten via PowerShell Set-Content (not the Edit tool) because this session's sandbox blocks read access to dotenv files; content is a known superset of VARIABLES.md keys + 2B additions (S3_*, WEBSUB_*, ADMIN_INGEST_TOKEN, YOUTUBE_CHANNEL_ID, MinIO ports).
- [2026-07-16] 2C pipeline stages read their inputs from and write their outputs to Postgres (not from BullMQ child job return values) so every stage is independently idempotent + resumable (§8.2) — each skips when its output already exists. Stages are DI factories (StageFactory) so the whole chain is unit-testable with fixture providers (zero real AI calls), matching the 2A discipline.
- [2026-07-16] og-image renders a self-contained 1200×630 SVG (no Satori/resvg dependency yet) instead of Satori→PNG, because the Anek Malayalam font asset + resvg native binding are heavier than 2C needs and og-image isn't in the 2C persistence exit check; Phase 3D upgrades it to Satori→PNG with the real font.
- [2026-07-16] index-search always builds the Meilisearch doc via the core mapper but only upserts when a Meili master key is configured (index config is Phase 4); Meilisearch is a rebuildable projection so skipping the upsert in 2C loses nothing.
- [2026-07-16] RelatedEdge writes are symmetric (from→to and to→from) so watch-next works from either video without a second pass; co-watch weight is 0 until WatchProgress exists (Phase 5).
- [2026-07-16] Full-chain persistence is proven by a vitest integration test guarded on DATABASE_URL (skips in CI where there's no Postgres); it ran green against the dev compose DB — Transcript/Enrichment/Article/Chapters/segment-vectors/RelatedEdge + DRAFT all persisted using fixture providers.
- [2026-07-16] Better Auth pinned to ~1.2.x because 1.3+ requires zod v4 (peer) and the repo is on zod 3; upgrading zod is out of scope for 2D. The TwoFactor table stays in the schema but the twoFactor plugin is deferred to Phase 6 (§10 enforcement) — it tripped a zod3/zod4 type mismatch via better-call and isn't needed for 2D login.
- [2026-07-16] RBAC role lives on Profile.role (not on the Better Auth user); a Better Auth databaseHook auto-creates a VIEWER Profile on user.create. A single authorize(minRole) layer in lib/authz.ts is used by BOTH admin pages and API routes (§10 defense against route-handler drift): 401 unauthenticated, 403 forbidden.
- [2026-07-16] Admin cache invalidation uses Next 16's updateTag(tag) (read-your-own-writes server-action API) rather than revalidateTag(tag, profile) which now requires a cache profile; the full publish fan-out (sitemap/IndexNow/RSS/og-image, §9.2) lands in Phase 3C.
- [2026-07-16] /login lives outside the (admin)/admin gated layout (path /login) so the layout's redirect-to-login can't loop; the admin layout redirects unauthenticated→/login and renders a 403 panel for signed-in non-editors.
- [2026-07-16] scripts/create-admin.ts builds its own minimal Better Auth instance (no server-only / next-js plugins) so it runs as a plain tsx script; it signs up through the auth API (real password hash) then elevates Profile→ADMIN, replacing the db-seed's credential-less placeholder admin.
- [2026-07-16] /admin/videos DataTable is server-rendered (searchParams status filter) rather than TanStack Table for 2D — simplest maintainable option; the interactive TanStack DataTable + EnrichmentDiff land in Phase 6 admin completion.
- [2026-07-16] Watch page (3A) uses a shared client PlayerProvider context (facade YouTube player → injects the IFrame API on first interaction, keeping LCP the thumbnail). ChapterList/TranscriptView/FaqAccordion command the player via context.seekTo and read context.currentTime; the whole watch UI is one client island (SSR'd for SEO) fed by a server data fetch — simplest way to share one player across many synced components.
- [2026-07-16] StickyPlayer is a compact control bar (the video keeps playing in the hero; audio continues offscreen) rather than an iframe-reparented visual mini-player — DOM reparenting of the YT iframe is fragile and is deferred to Phase 3D polish.
- [2026-07-16] TranscriptView renders the full segment list inside a scroll container; true windowed virtualization is deferred to Phase 3D (perf gate) — seeded transcripts are short and correctness/sync matter more now.
- [2026-07-16] Funnel analytics: POST /api/v1/events sets an anonymous viewerKey cookie ("a:{uuid}", §2) and writes AnalyticsEvent; client emits via navigator.sendBeacon (keepalive fetch fallback). Event names centralized in core (FUNNEL_EVENTS). Verified end-to-end against the running build (row written with anon viewerKey).
- [2026-07-16] Watch page ISR = revalidate 300 + generateStaticParams(publishedSlugs); tag-based revalidateTag(video:{id}) + full JSON-LD/OG-image SEO land in Phase 3C. `motion` added to apps/web deps (previously only in packages/ui).
- [2026-07-20] Next 16 passes the sitemap shard `id` as a Promise (like `params`); the sync destructure made every shard fall through to the last branch. Fixed by awaiting props.id — verified each shard serves distinct content.
- [2026-07-20] generateSitemaps does not emit a /sitemap.xml index, so robots.txt lists the four shard URLs directly (videos/articles/topics/pages) and seo-ping re-pings the videos shard.
- [2026-07-20] Redirect-table 301s run in edge middleware via a cached fetch to an internal nodejs route (Prisma cannot run on the edge). Route is force-dynamic; middleware caches the map 5 min in-process and fails open so a lookup error never blocks a request.
- [2026-07-25] Perf (3D): deferred cmdk omnibox (load-on-open) + Motion components (dynamic ssr:false) + below-fold islands (LazyInView IntersectionObserver) + tree-shaking (sideEffects:false on ui/core, optimizePackageImports); next/image AVIF/WebP for the hero thumbnail (priority). CLS measured 0.005.
- [2026-07-25] Local Lighthouse LCP/TBT/perf are unmeasurable on this Windows box (Chrome+LH12 trace RootCauses/frame_sequence bug → NaN); CLS + script-size do report. CI runs on Linux where traces work. CI gate: CLS<=0.05 + LCP<=2000ms + perf>=0.90 BLOCKING (error), accessibility + script-size(170KB) as warn. §11 north-star (perf95/LCP1800/JS170KB): CWV met; strict JS-byte budget (currently ~185KB initial incl. ~70KB React framework) deferred to a dedicated splitting pass.
- [2026-07-25] CI now provisions pgvector Postgres 17 + Redis 7 services, runs prisma migrate deploy + seed before build (SSG pages query the DB at build); e2e-perf job flipped from continue-on-error to BLOCKING (Playwright happy-path + LHCI budgets).
- [2026-07-31] Search (4A): centralized the Meilisearch module in packages/core/search (config/index-settings, article/topic/faq mappers, script classifier, SearchClient); client is server-only via the "@vaidyasala/core/search/client" subpath so client bundles never pull meilisearch. Worker meili port + reindex + /api/v1/search all share it.
- [2026-07-31] reindex.ts lives in infra/scripts and is run from repo root (root search:reindex + @vaidyasala/db,@vaidyasala/core added as root devDeps) because Node ESM resolves bare specifiers from the importing file location — infra/ has no node_modules, so the packages must link into the repo-root node_modules.
- [2026-07-31] Search degrades gracefully when MEILI_MASTER_KEY is absent (searchClient null → empty grouped results, query still logged), so CI needs no Meilisearch service.
- [2026-08-04] AI answer (4B): SSE route precomputes all events then emits via ReadableStream.pull — the eager start()+close() pattern raced Next 16 pipe attach (ERR_INVALID_STATE "failed to pipe") on the fast no-answer path.
- [2026-08-04] AI-answer retrieval strips question/stop-words and, because Meili default matchingStrategy drops the trailing keyword, also unions per-content-token video hits — so English questions ("how to control cholesterol?") reach the right video whose transcript segments are Malayalam.
- [2026-08-04] Fixture-mode AI answer is extractive (top retrieved segments stitched) with real [videoId,startSec] citations; live Claude/embeddings used when keys present. Retrieval always grounded — never model-memory. web typecheck now runs `next typegen` first so new routes are in routes.d.ts before tsc (CI-safe).
- [2026-08-05] Engagement (5): resume-at-position is resolved client-side (URL ?t= wins, else GET /api/v1/progress) so /watch stays static (server cookie-read would force it dynamic + break ISR/perf). WatchProgress viewerKey merges anon→user on first authenticated getViewerKey (idempotent, keeps furthest position). Progress beacon posts every ~10s + on pagehide (sendBeacon).
- [2026-08-05] Comments held PENDING; visible only after EDITOR approve (/admin/comments). Turnstile verify skips when TURNSTILE_SECRET absent (dev). PWA: manifest (SVG icon), sw.js (network-first navigations + rolling 10-page cache + /offline), install prompt after 2nd visit. public/** excluded from web eslint (SW globals).
- [2026-08-06] Admin (6): 2FA is self-contained RFC 6238 TOTP (lib/totp.ts, node:crypto HMAC-SHA1, ±1 window, base32) written to the existing TwoFactor table — better-auth's twoFactor plugin has version-skew type errors in ~1.2.7 (not assignable to BetterAuthPlugin), so it was dropped rather than pinned. Admin layout gates on user.twoFactorEnabled → redirect /2fa (bypass via ADMIN_2FA_ENFORCE=false for CI/e2e). /2fa lives outside admin/ layout to avoid a redirect loop.
- [2026-08-06] Admin form actions used directly in <form action={}> return Promise<void> (Next 16 types reject ActionResult there); their update/delete siblings (invoked from client transitions) keep returning ActionResult. Validation failures in the void actions early-return silently — the form re-renders unchanged.
- [2026-08-06] Ops nightly jobs (seo-pull/link-crawl/search-consistency) run on worker cron; seo-pull is BLOCKED (no GSC/CrUX creds) and stubbed. link-crawl HEAD-probes sitemap URLs → SiteHealthIssue; search-consistency diffs DB published count vs Meili numberOfDocuments → SiteHealthIssue on drift. Observability compose is a separate profile bound 127.0.0.1-only (prometheus/grafana/loki/uptime-kuma), never started by the app stack.

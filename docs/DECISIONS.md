# DECISIONS — Vaidyasala

Autopilot decisions log (LAW 1). Format: `- [date] [choice] because [reason]`.
- [2026-07-15] Authored LAW 7 + docs/PHASES.md Appendix A myself because no vaidyasala-PHASES*.md existed in Downloads and the prompt pack had no Appendix A; extracted phase prompts from PART 4 of the prompt pack and defined RUN AUTOPILOT as a sequential, resumable phase loop stopping before the human-gated Phase 7.
- [2026-07-15] docs/ARCHITECTURE.md sourced from Downloads/vaidyasala-architecture_2.md (latest of 3 identical copies) because Phase 1A onward requires it and LAW 5 forbids later modification.
- [2026-07-15] Dev compose host ports set to 55432/56379/57700 (pg/redis/meili) because 6379 and likely 5432 were already allocated on the local machine; bound to 127.0.0.1 only.
- [2026-07-15] Workspace packages consumed as TypeScript source (exports point at ./src, web transpilePackages, worker via tsx/tsc) instead of a per-package build step because it removes build ordering complexity for an internal monorepo.
- [2026-07-15] ioredis pinned via pnpm-workspace overrides + BullMQ given plain ConnectionOptions (not a shared Redis instance) because bullmq bundled a second ioredis version causing type-identity conflicts.
- [2026-07-15] typedRoutes disabled until Phase 3B because the nav/footer reference public routes (/topics, /latest, ...) that do not exist until Phase 3; re-enable when those pages land.
- [2026-07-15] Fonts loaded via next/font/google (Inter latin, Anek Malayalam malayalam+latin, Noto Serif Malayalam malayalam preload=false) instead of self-hosted WOFF2 because next/font auto-subsets, preloads, and emits size-adjusted fallback metrics (zero CLS) per §5.3.
- [2026-07-15] Tier-1 primitives hand-written on Radix + cmdk + sonner (not shadcn CLI) because the CLI cannot scaffold non-interactively into a workspace package; styled with @theme tokens.
- [2026-07-15] AI model tiers: workhorse=claude-sonnet-5, cheap=claude-haiku-4-5 (per §8.1 Sonnet/Haiku split, mapped to current model IDs via the claude-api reference).
- [2026-07-15] Structured AI output uses a manual completeJson(Zod + 2-retry repair) loop with providers accepting injected clients/fetch, rather than output_config.format, so the whole chain is unit-testable with mocked providers and makes zero real API calls in CI.
- [2026-07-16] Media storage abstracted behind an S3-compatible StoragePort (@aws-sdk/client-s3); dev uses a scoped MinIO service in compose.dev.yml (vaidyasala-dev-minio, 127.0.0.1:59000) instead of BLOCKING on R2 creds, because Docker is available locally and it makes ingest media upload runnable now; prod swaps the endpoint to R2. Storage auto-creates the media bucket on first put.
- [2026-07-16] Ingest metadata: prefer YouTube Data API when YOUTUBE_API_KEY is present, else fall back to `yt-dlp -J` (no key required), so ingest can run without an API key. yt-dlp/ffmpeg also power audio extraction; absent binaries mark the audio step BLOCKED without sinking ingest.
- [2026-07-16] Queue contract (QUEUE_NAMES, PIPELINE_STAGES §8.2 order, idempotencyKey, parseYouTubeId, ingestInputSchema) lives in packages/core/queue (pure, no bullmq/ioredis) so apps/web (enqueue) and apps/worker (consume) share one contract without web importing worker (§3).
- [2026-07-16] Per-video pipeline is a BullMQ FlowProducer chain nested so the leaf (asr) runs first and completion bubbles to the root (quality-gate). Ingest enqueues it only once ≥1 stage processor is registered (none until 2C), so 2B ingest stops cleanly at PROCESSING instead of faking downstream success or parking DLQ noise.
- [2026-07-16] DLQ = dedicated `vaidyasala.dlq` queue; a Worker 'failed' listener copies terminally-failed jobs (attemptsMade ≥ attempts) there for admin visibility. Retries use exponential backoff ×5 (JOB_OPTS).
- [2026-07-16] .env.example was rewritten via PowerShell Set-Content (not the Edit tool) because this session's sandbox blocks read access to dotenv files; content is a known superset of VARIABLES.md keys + 2B additions (S3_*, WEBSUB_*, ADMIN_INGEST_TOKEN, YOUTUBE_CHANNEL_ID, MinIO ports).
- [2026-07-16] 2C pipeline stages read their inputs from and write their outputs to Postgres (not from BullMQ child job return values) so every stage is independently idempotent + resumable (§8.2) — each skips when its output already exists. Stages are DI factories (StageFactory) so the whole chain is unit-testable with fixture providers (zero real AI calls), matching the 2A discipline.
- [2026-07-16] og-image renders a self-contained 1200×630 SVG (no Satori/resvg dependency yet) instead of Satori→PNG, because the Anek Malayalam font asset + resvg native binding are heavier than 2C needs and og-image isn't in the 2C persistence exit check; Phase 3D upgrades it to Satori→PNG with the real font.
- [2026-07-16] index-search always builds the Meilisearch doc via the core mapper but only upserts when a Meili master key is configured (index config is Phase 4); Meilisearch is a rebuildable projection so skipping the upsert in 2C loses nothing.
- [2026-07-16] RelatedEdge writes are symmetric (from→to and to→from) so watch-next works from either video without a second pass; co-watch weight is 0 until WatchProgress exists (Phase 5).
- [2026-07-16] Full-chain persistence is proven by a vitest integration test guarded on DATABASE_URL (skips in CI where there's no Postgres); it ran green against the dev compose DB — Transcript/Enrichment/Article/Chapters/segment-vectors/RelatedEdge + DRAFT all persisted using fixture providers.
- [2026-07-16] Better Auth pinned to ~1.2.x because 1.3+ requires zod v4 (peer) and the repo is on zod 3; upgrading zod is out of scope for 2D. The TwoFactor table stays in the schema but the twoFactor plugin is deferred to Phase 6 (§10 enforcement) — it tripped a zod3/zod4 type mismatch via better-call and isn't needed for 2D login.
- [2026-07-16] RBAC role lives on Profile.role (not on the Better Auth user); a Better Auth databaseHook auto-creates a VIEWER Profile on user.create. A single authorize(minRole) layer in lib/authz.ts is used by BOTH admin pages and API routes (§10 defense against route-handler drift): 401 unauthenticated, 403 forbidden.
- [2026-07-16] Admin cache invalidation uses Next 16's updateTag(tag) (read-your-own-writes server-action API) rather than revalidateTag(tag, profile) which now requires a cache profile; the full publish fan-out (sitemap/IndexNow/RSS/og-image, §9.2) lands in Phase 3C.
- [2026-07-16] /login lives outside the (admin)/admin gated layout (path /login) so the layout's redirect-to-login can't loop; the admin layout redirects unauthenticated→/login and renders a 403 panel for signed-in non-editors.
- [2026-07-16] scripts/create-admin.ts builds its own minimal Better Auth instance (no server-only / next-js plugins) so it runs as a plain tsx script; it signs up through the auth API (real password hash) then elevates Profile→ADMIN, replacing the db-seed's credential-less placeholder admin.
- [2026-07-16] /admin/videos DataTable is server-rendered (searchParams status filter) rather than TanStack Table for 2D — simplest maintainable option; the interactive TanStack DataTable + EnrichmentDiff land in Phase 6 admin completion.
- [2026-07-16] Watch page (3A) uses a shared client PlayerProvider context (facade YouTube player → injects the IFrame API on first interaction, keeping LCP the thumbnail). ChapterList/TranscriptView/FaqAccordion command the player via context.seekTo and read context.currentTime; the whole watch UI is one client island (SSR'd for SEO) fed by a server data fetch — simplest way to share one player across many synced components.
- [2026-07-16] StickyPlayer is a compact control bar (the video keeps playing in the hero; audio continues offscreen) rather than an iframe-reparented visual mini-player — DOM reparenting of the YT iframe is fragile and is deferred to Phase 3D polish.
- [2026-07-16] TranscriptView renders the full segment list inside a scroll container; true windowed virtualization is deferred to Phase 3D (perf gate) — seeded transcripts are short and correctness/sync matter more now.
- [2026-07-16] Funnel analytics: POST /api/v1/events sets an anonymous viewerKey cookie ("a:{uuid}", §2) and writes AnalyticsEvent; client emits via navigator.sendBeacon (keepalive fetch fallback). Event names centralized in core (FUNNEL_EVENTS). Verified end-to-end against the running build (row written with anon viewerKey).
- [2026-07-16] Watch page ISR = revalidate 300 + generateStaticParams(publishedSlugs); tag-based revalidateTag(video:{id}) + full JSON-LD/OG-image SEO land in Phase 3C. `motion` added to apps/web deps (previously only in packages/ui).
- [2026-07-20] Next 16 passes the sitemap shard `id` as a Promise (like `params`); the sync destructure made every shard fall through to the last branch. Fixed by awaiting props.id — verified each shard serves distinct content.
- [2026-07-20] generateSitemaps does not emit a /sitemap.xml index, so robots.txt lists the four shard URLs directly (videos/articles/topics/pages) and seo-ping re-pings the videos shard.
- [2026-07-20] Redirect-table 301s run in edge middleware via a cached fetch to an internal nodejs route (Prisma cannot run on the edge). Route is force-dynamic; middleware caches the map 5 min in-process and fails open so a lookup error never blocks a request.
- [2026-07-25] Perf (3D): deferred cmdk omnibox (load-on-open) + Motion components (dynamic ssr:false) + below-fold islands (LazyInView IntersectionObserver) + tree-shaking (sideEffects:false on ui/core, optimizePackageImports); next/image AVIF/WebP for the hero thumbnail (priority). CLS measured 0.005.
- [2026-07-25] Local Lighthouse LCP/TBT/perf are unmeasurable on this Windows box (Chrome+LH12 trace RootCauses/frame_sequence bug → NaN); CLS + script-size do report. CI runs on Linux where traces work. CI gate: CLS<=0.05 + LCP<=2000ms + perf>=0.90 BLOCKING (error), accessibility + script-size(170KB) as warn. §11 north-star (perf95/LCP1800/JS170KB): CWV met; strict JS-byte budget (currently ~185KB initial incl. ~70KB React framework) deferred to a dedicated splitting pass.
- [2026-07-25] CI now provisions pgvector Postgres 17 + Redis 7 services, runs prisma migrate deploy + seed before build (SSG pages query the DB at build); e2e-perf job flipped from continue-on-error to BLOCKING (Playwright happy-path + LHCI budgets).
- [2026-07-31] Search (4A): centralized the Meilisearch module in packages/core/search (config/index-settings, article/topic/faq mappers, script classifier, SearchClient); client is server-only via the "@vaidyasala/core/search/client" subpath so client bundles never pull meilisearch. Worker meili port + reindex + /api/v1/search all share it.
- [2026-07-31] reindex.ts lives in infra/scripts and is run from repo root (root search:reindex + @vaidyasala/db,@vaidyasala/core added as root devDeps) because Node ESM resolves bare specifiers from the importing file location — infra/ has no node_modules, so the packages must link into the repo-root node_modules.
- [2026-07-31] Search degrades gracefully when MEILI_MASTER_KEY is absent (searchClient null → empty grouped results, query still logged), so CI needs no Meilisearch service.
- [2026-08-04] AI answer (4B): SSE route precomputes all events then emits via ReadableStream.pull — the eager start()+close() pattern raced Next 16 pipe attach (ERR_INVALID_STATE "failed to pipe") on the fast no-answer path.
- [2026-08-04] AI-answer retrieval strips question/stop-words and, because Meili default matchingStrategy drops the trailing keyword, also unions per-content-token video hits — so English questions ("how to control cholesterol?") reach the right video whose transcript segments are Malayalam.
- [2026-08-04] Fixture-mode AI answer is extractive (top retrieved segments stitched) with real [videoId,startSec] citations; live Claude/embeddings used when keys present. Retrieval always grounded — never model-memory. web typecheck now runs `next typegen` first so new routes are in routes.d.ts before tsc (CI-safe).
- [2026-08-05] Engagement (5): resume-at-position is resolved client-side (URL ?t= wins, else GET /api/v1/progress) so /watch stays static (server cookie-read would force it dynamic + break ISR/perf). WatchProgress viewerKey merges anon→user on first authenticated getViewerKey (idempotent, keeps furthest position). Progress beacon posts every ~10s + on pagehide (sendBeacon).
- [2026-08-05] Comments held PENDING; visible only after EDITOR approve (/admin/comments). Turnstile verify skips when TURNSTILE_SECRET absent (dev). PWA: manifest (SVG icon), sw.js (network-first navigations + rolling 10-page cache + /offline), install prompt after 2nd visit. public/** excluded from web eslint (SW globals).
- [2026-08-06] Admin (6): 2FA is self-contained RFC 6238 TOTP (lib/totp.ts, node:crypto HMAC-SHA1, ±1 window, base32) written to the existing TwoFactor table — better-auth's twoFactor plugin has version-skew type errors in ~1.2.7 (not assignable to BetterAuthPlugin), so it was dropped rather than pinned. Admin layout gates on user.twoFactorEnabled → redirect /2fa (bypass via ADMIN_2FA_ENFORCE=false for CI/e2e). /2fa lives outside admin/ layout to avoid a redirect loop.
- [2026-08-06] Admin form actions used directly in <form action={}> return Promise<void> (Next 16 types reject ActionResult there); their update/delete siblings (invoked from client transitions) keep returning ActionResult. Validation failures in the void actions early-return silently — the form re-renders unchanged.
- [2026-08-06] Ops nightly jobs (seo-pull/link-crawl/search-consistency) run on worker cron; seo-pull is BLOCKED (no GSC/CrUX creds) and stubbed. link-crawl HEAD-probes sitemap URLs → SiteHealthIssue; search-consistency diffs DB published count vs Meili numberOfDocuments → SiteHealthIssue on drift. Observability compose is a separate profile bound 127.0.0.1-only (prometheus/grafana/loki/uptime-kuma), never started by the app stack.
- [2026-08-06] Phase 7-PRE could NOT run: ssh to 194.164.151.202 is blocked by the local agent permission layer, so zero commands touched the server. SERVER-AUDIT.md is written as an explicit NO-GO stub carrying the exact read-only command block, rather than being filled with plausible-looking numbers — a guessed WEB_PORT on a box with 10 live sites is a collision that takes someone else's site down (LAW 6). 7A artifacts were authored offline; nothing was deployed.
- [2026-08-06] Worker containerisation: apps/worker is bundled with esbuild (`build:bundle` → dist-bundle/main.js, workspace TS inlined, real npm packages external) instead of shipping `tsc` output. Reason: @vaidyasala/core and @vaidyasala/db declare their `exports` as raw .ts (the web app consumes them through Next transpilePackages), so `node dist/main.js` cannot resolve them at runtime — the existing `start` script was broken outside tsx. @anthropic-ai/sdk and @prisma/client added to worker deps (they were reached only transitively through core/db, which pnpm's strict node_modules does not expose). Verified: bundle boots to "[worker] boot" and fails only on Redis ECONNREFUSED.
- [2026-08-06] compose.prod.yml requires WEB_PORT with `${WEB_PORT:?}` (no default) and setup-vaidyasala.sh re-verifies the port is free via `ss` + `docker ps` at run time, aborting before any write. The audit is a snapshot and can be stale by deploy time; on a shared host the runtime check is the real guard. Deploy workflow diffs non-vaidyasala `docker ps` output before/after and fails the run if anything outside our project changed.
- [2026-08-06] §10's origin firewall lock (Cloudflare-IP allowlist) is SKIPPED on this shared VPS — it needs ufw/iptables changes affecting all 10 sites (LAW 6 forbids). Compensating control: our vhost answers only the vaidhyasala.com Host header. The firewall allowlist is listed as a human decision in GO-LIVE-MANUAL.md.
- [2026-08-08] Sarvam was ALREADY the primary ASR provider (pipeline/deps.ts wires SarvamAsrProvider; WhisperAsrProvider is an unused §8.1 fallback class) — there was no Whisper to replace. Rather than add a duplicate module at the requested packages/core/sarvam/client.ts, the real implementation moved to packages/core/src/sarvam/client.ts (exported as @vaidyasala/core/sarvam) and ai/providers/sarvam.ts became a thin AsrProvider adapter over it, so there is one request/response mapping, not two. Whisper is left in place for backward compatibility.
- [2026-08-08] Sarvam "streaming progress" is implemented as batch-job polling (submit /speech-to-text-job → poll status → collect), not a socket: Sarvam has no server-push stream for pre-recorded audio. transcribe() auto-selects the job path past 300s of audio because long clips through the sync endpoint hit gateway timeouts. Progress ratio prefers the provider's own percentage and otherwise approaches 0.9 asymptotically so the bar always moves but never claims completion early. AsrProvider.transcribe gained an OPTIONAL third onProgress param — additive, since TS lets a 2-param implementation satisfy a 3-param signature (whisper is untouched).
- [2026-08-08] Haiku is now the default LLM tier: `task.tier === "workhorse" ? sonnet : haiku` (was: default sonnet). Every prompt in ai/prompts declares its tier explicitly, so only unlabelled ad-hoc tasks change cost. The ai.test.ts case asserting the old default was updated and a companion case added asserting explicit workhorse still gets Sonnet.
- [2026-08-08] Mixpanel is a SECOND sink behind the existing first-party /api/v1/events, not a replacement — emitEvent() mirrors into it. The SDK is dynamically imported at requestIdleCallback (never in the initial bundle, ~60KB gz) and no-ops entirely without NEXT_PUBLIC_MIXPANEL_TOKEN or under DNT/GPC, so a blocked Mixpanel can never cost a funnel event or block the player on mobile. npm package is `mixpanel-browser`; the requested `@mixpanel/browser` does not exist. Search text is NOT sent — only query_length — because people search this site for symptoms.
- [2026-08-08] New funnel event names (pause, seek, search, search_result_click) needed NO migration: AnalyticsEvent.name is a plain String column, so the change is confined to the Zod enum. Seek is derived by watching the 1s poll for jumps >2.5s, since the YouTube IFrame API emits no seek event.
- [2026-08-08] Worker Sentry capture lives in runMirrored() — the single chokepoint every stage passes through — so pipeline errors get stage/video/attempt context in one place instead of per-job try/catch. Events are fingerprinted by stage+message so one provider fault does not split into many issues. @sentry/node and @opentelemetry/* are esbuild EXTERNALS: bundling them inflated dist-bundle from 74.5kb to 1.5mb and breaks Sentry's require-hook auto-instrumentation.
- [2026-08-08] Sentry browser traces sample at 5% (server 20%) and session replay is off — mobile is the primary surface and replay is the most expensive integration. tunnelRoute:"/monitoring" routes SDK traffic through our origin so content blockers do not eat mobile error reports. Video time-to-first-frame and rebuffer counts are recorded explicitly because CWV stops measuring at LCP, before the player mounts.
- [2026-08-08] Phase 2A: /watch/[slug] ALREADY existed (built in Phases 3+5) with player, chapters+active-highlight, transcript ML/EN toggle + click-seek + playhead highlight, AI summary/takeaways/FAQ, comments, share, related rail, progress sync and full JSON-LD. So 2A was scoped as gap-filling, not a rebuild: transcript search, English summary surfacing, like/bookmark, mobile collapse + comments bottom sheet, X/Twitter share, and the two named API routes. No parallel chapter-navigator.tsx / transcript-viewer.tsx were created — that would have duplicated working components; the existing chapter-list.tsx / transcript-view.tsx were extended instead.
- [2026-08-08] Subscribe CTA: §6.1 gates the banner at 75% watched ("earn it"), but Phase 2A requires it visible at all times. Resolved by keeping SubscribeOverlay's 75% banner AND adding a persistent quieter CTA — SubscribeStickyBar (mobile, fixed above the safe area, mr-16 to clear the floating like/save buttons) and SubscribeStickyAside (desktop, sticky in the sidebar). Two exports rather than one breakpoint-switching component, because the mobile copy is viewport-fixed and the desktop copy sits in the sidebar's flow — different parents.
- [2026-08-08] Mobile collapse is built INTO transcript-view.tsx and chapter-list.tsx (their existing <h2> becomes the toggle under md/lg) rather than a wrapper component. A first attempt used a generic CollapsiblePanel wrapper, which rendered a second "Transcript"/"Chapters" heading on top of each section's own — the duplicate was caught by the watch e2e test resolving to the hidden toggle span. Wrapper deleted.
- [2026-08-08] Migration is strictly additive: WatchProgress.watchedPercentage INTEGER NOT NULL DEFAULT 0, plus a new VideoReaction table (viewerKey+videoId unique, no FK to Video — mirrors WatchProgress so anonymous reactions survive sign-in via the existing viewerKey merge). No column or table was dropped or altered in a lossy way. `resumeAt` was NOT added: positionSec already is the resume point.
- [2026-08-08] /api/videos/[id]/watch-progress and /api/v1/progress share one write path (lib/progress.recordProgress) instead of two implementations — the v1 route is the 204 sendBeacon endpoint, the new one is the addressable JSON form. Position and watchedPercentage only ever advance (verified: POST 120 then POST 30 stays 120), since beacons arrive out of order and scrubbing back does not un-watch. /api/videos/[id]/enrichment accepts id OR slug because callers coming from a URL have the slug; the watch page does not use it (enrichment is server-rendered for SEO).
- [2026-08-08] ISR on /watch raised 300s → 3600s as specified. Safe because publish/edit busts the cache immediately via revalidateTag; the longer window only delays view-count drift.
- [2026-08-08] Phase 2B: the home page already existed with featured/trending/latest/recommended/continue/topics/newsletter, so 2B was gap-filling. Named-path deviations, all deliberate: (a) app/page.tsx NOT created — route "/" already resolves from app/(public)/page.tsx and a second file for the same route is a build error; (b) NO Category model created — the schema's concept is Topic, and /api/categories serves topics rather than splitting one concept across two tables; (c) NO EmailSubscriber model created — NewsletterSubscriber already exists with a working double-opt-in form; (d) no "creator" relation exists to join (single-channel product).
- [2026-08-08] Home page was ALREADY dynamic (ƒ) before this phase — ContinueIsland read the viewer cookie during render via getContinueWatching→getViewerKey→cookies(), which opts the whole route out of ISR. Verified by rebuilding the pre-change page. So `export const revalidate` was inert on this route. Fixed by moving continue-watching to a client component fetching GET /api/v1/continue after hydration (same pattern as the watch page's resume position); RecommendedIsland reads no cookie and stays server-rendered. Home is now ○ Static with 30m revalidate, which is what the <2s-on-4G budget depends on.
- [2026-08-08] Video.featuredAt DateTime? (additive, indexed desc) gives editors manual hero selection; newest featuredAt wins so featuring is a single write with no need to clear the previous pick, and null everywhere falls back to the existing qualityScore ordering so the hero is never empty. Admin toggle added to PublishControls (published videos only) writing an AuditLog and revalidating "/".
- [2026-08-08] video-carousel.tsx uses native overflow + CSS scroll-snap for scrolling (momentum, a11y and RTL for free, zero JS on mobile) with arrow buttons layered on as a desktop-only affordance, rendered only when there is somewhere to scroll. Category icons are matched by regex on slug/nameEn rather than a new Topic.icon column — otherwise every new topic looks broken until an editor picks an icon; unmatched topics fall back to a stethoscope.
- [2026-08-08] Design brief items not applied, with reasons: "50x200px thumbnails" is not a usable aspect ratio for 16:9 video stills (kept VideoCard's existing sizes); "dark theme consistent with the ingestion page" — no ingestion page exists in this app and the site already has its own theme tokens, so existing tokens were used.
- [2026-08-08] Phase 2C: /topics and /topics/[slug] already existed with names, descriptions, video grids, articles, FAQs, JSON-LD (collectionPage + medicalCondition + breadcrumb) and sitemap coverage. Gaps filled: topic-name search on the index, icons+descriptions on the cards, related topics, sort (latest/trending/duration), duration filter, 12-per-page pagination, and a VISIBLE breadcrumb (only the JSON-LD existed before — invisible to actual readers). ISR 600 → 3600 on both.
- [2026-08-08] Sort/filter/pagination live in lib/topic-browse.ts as pure functions used by BOTH the client controls and /api/categories/[slug]/videos, so the UI and API cannot disagree about what "short" or "trending" means. Deliberately NOT driven by server searchParams: reading them during render opts the route out of ISR — the same trap that was silently making the home page dynamic (see 2B). The page ships its full video list and the client sorts/pages in memory, which also makes switching instant. Revisit if a topic ever exceeds a few hundred videos; the API route already supports paging for that case.
- [2026-08-08] Duration bands: short <10min, medium 10–30 inclusive at both ends, long >30. Boundaries are covered by unit tests so no video can fall between bands. Pagination clamps an out-of-range page to the last valid one rather than 404ing, because changing a filter can shrink the page count under the reader's current page.
- [2026-08-08] Related topics rank by SHARED VIDEOS (TopicVideo overlap = a real content relationship), then top up with same-kind topics, then any topic by catalogue size. The same-kind-only fallback was tried first and rendered an empty section on the seed data (one topic per kind) — verified against a running server, then widened. "Category" remains an API-level alias for Topic; no Category model exists or was created.
- [2026-08-08] Phase 2D: search was already built in Phase 4A+4B — ⌘K palette (cmdk, lazy), search input in the top bar, debounced autocomplete, /search?q= page, Meilisearch with weighted searchable attributes across title/keywords/summary/chapters/transcript, Manglish classification + transliteration, pgvector semantic answers, SearchQueryLog + /admin/search-analytics. Real gaps filled: filters (duration, publish date), sort (relevance/date/views), popular-search suggestions, real topic suggestions on zero results, and a full-screen mobile palette.
- [2026-08-08] Filter/sort vocabulary lives in packages/core/src/search/filters.ts as pure functions (buildVideoFilter/buildVideoSort) shared by the Meili client, the API route and the UI — same pattern as lib/topic-browse, and the duration bands are deliberately identical so "short" means the same thing in search and on a topic hub. Filters apply to the VIDEOS index only: a topic or FAQ has no length or publish date. "relevance" returns no sort array at all, because passing one would override Meili's own ranking.
- [2026-08-08] Meili rejects filtering/sorting on undeclared attributes, so INDEX_SETTINGS.videos gained filterable durationSec/publishedAt and sortable publishedAt/viewCount/durationSec. viewCount is NEW in the video doc (read defensively from the free-form Video.stats JSON in both the worker's index-search job and infra/scripts/reindex.ts) and defaults to 0 rather than null — Meili sorts missing values last in BOTH directions, which would park unstatted videos at the top of an ascending sort. Requires a reindex to take effect; run on deploy.
- [2026-08-08] /api/search was NOT created — /api/v1/search already exists with rate limiting and query logging, and a second public search URL doing the same thing is cruft. The existing route was extended with duration/date/sort/topicSlug instead. Unknown values 422 at the API but fall back to defaults on the /search page, so a hand-edited URL still returns results. Zero-result responses now carry fallbackTopics.
- [2026-08-08] Popular searches come from SearchQueryLog filtered to results > 0 — suggesting a query we know is a dead end is worse than suggesting nothing — and are de-duplicated on a case/whitespace-normalised key so near-variants of one term do not fill the list. "creator" filter from the brief was skipped: single-channel product, no Creator model to filter on.
- [2026-08-08] Docker build fixed WITHOUT replacing pnpm with npm — the premise did not hold. Measured: `npm install` in this repo fails with EUNSUPPORTEDPROTOCOL ("Unsupported URL Type workspace:") because all 10 internal deps use `workspace:*`, and npm reads workspaces from package.json (absent) not pnpm-workspace.yaml. Switching would mean rewriting every internal dep, adding a workspaces field, generating package-lock.json and maintaining it alongside pnpm-lock.yaml. The ACTUAL failure was the install method: `npm install -g pnpm` inside the workspace makes pnpm verify its own native binary against the lockfile → "Cannot verify the identity of the @pnpm/exe.linux-x64 native binary". Fixed by using corepack, which takes the pinned version from package.json `packageManager`.
- [2026-08-08] Four further Docker bugs found by actually building (these images had NEVER been built): (1) no .dockerignore, so `COPY . .` shipped the host's Windows-built node_modules into the image; (2) copying pnpm's node_modules across build stages broke Turbopack's internal next/font resolution — install now happens in the build stage, manifests-first so the layer still caches; (3) the worker runner flattened the tree to /app/dist-bundle, but pnpm's symlinks are RELATIVE (../../node_modules/.pnpm/…), so the image built and then died at boot with "Cannot find package 'bullmq'" — the runner now mirrors the repo layout; (4) UTF-8 BOM before FROM (tolerated by this Docker version, removed anyway).
- [2026-08-08] `next build` prerenders DB-backed pages, so the web image needs a database at build time. generateStaticParams is now wrapped in lib/static-params.ts (safeStaticParams) which degrades to on-demand rendering when the DB is unreachable — every such route already sets dynamicParams=true, so pages are generated on first request and cached by ISR. Top-level page queries (e.g. /latest) still need a DB, so DATABASE_URL is a build ARG (not ENV — it must not be baked into the runner image) and deploy.yml passes secrets.BUILD_DATABASE_URL. Verified end-to-end: web image 117MB serves /api/health, / and /watch/[slug] as non-root with no `next start` warning; worker image boots to "[worker] ready · workers=3 · pipelineStages=10" with yt-dlp 2026.07.04 and ffmpeg 5.1.9 present.
- [2026-08-08] "Don't prerender DB-backed pages during the Docker build" is now solved WITHOUT making pages dynamic. packages/db/src/build-guard.ts wraps the Prisma client so that, during `next build` ONLY (NEXT_PHASE=phase-production-build), connection failures on READ operations return empty instead of throwing. Deliberately narrow — build phase only, connection/initialisation errors only, reads only — because silently swallowing DB errors at runtime or on writes would be indefensible. Dockerfile.web defaults DATABASE_URL to an unreachable placeholder (Prisma validates the var exists) so `docker build` needs no database at all. Verified: image builds with zero DB, 117MB.
- [2026-08-08] MEASURED CONSEQUENCE of a no-DB build: the listing pages prerender EMPTY and keep serving that HTML until their revalidate window elapses — home showed 0 video links on both first and second request. So a hermetic build alone is not safe. Added POST /api/internal/revalidate (token-gated via REVALIDATE_TOKEN, falling back to ADMIN_INGEST_TOKEN; 503 when unset rather than open, 401 on a bad token) which revalidatePath()s the six listing routes, and the deploy job calls it after the smoke check. Verified end-to-end: 0 video links before → 5 videos and 3 topics after. Passing a real DATABASE_URL at build time remains the better path (real content baked in); this is the safety net for CI builds that have no database.
- [2026-08-08] `revalidate = 0` on the home page was requested but NOT applied: it means "never cache / fully dynamic", not on-demand ISR, and it would undo the Phase 2B fix that made the home page static (the whole basis of the <2s-on-4G budget). The home page also was not a build blocker — it builds fine either way. Also note the requested path apps/web/app/page.tsx does not exist; home lives at apps/web/app/(public)/page.tsx behind a route group. Auth route DID get `export const dynamic = "force-dynamic"` as asked (correct — sessions/cookies/OAuth callbacks are per-request).
- [2026-08-09] "Runner stage missing package.json/pnpm-lock.yaml/packages" was diagnosed as the cause of "Could not find Prisma Schema" — but inspecting the built worker image showed the schema IS present (packages/db/prisma/{schema,migrations,seed.ts}). The real fault was in deploy.yml: it invoked the Prisma CLI at node_modules/prisma/build/index.js, but `prisma` is a devDependency of @vaidyasala/db, so under pnpm it resolves at packages/db/node_modules/prisma/build/index.js. Fixed with a two-path loop so a node-linker change cannot silently break deploys. Verified by running `migrate deploy` from the built image against a live DB: "5 migrations found ... No pending migrations to apply."
- [2026-08-09] The proposed runner COPY lines were NOT applied. `COPY --from=build /repo/apps/web/prisma ./packages/db/prisma` references a path that does not exist (the schema lives at packages/db/prisma) and would fail the build. `COPY packages ./packages` from the build context would ship raw TypeScript with no node_modules into the runner. pnpm-lock.yaml in a runner is inert — nothing installs there. And the web runner is Next standalone output with no package manager, so the suggested verification `docker compose exec web pnpm prisma migrate deploy` cannot work no matter what is copied in; migrations run from the worker image, which is what deploy.yml does.
- [2026-08-10] Phase 2E as written targets a DIFFERENT PRODUCT — a doctor-booking marketplace (practitioner experience levels, consultation type, availability slots, INR price ranges, distance/geolocation, "No doctors found", saved_doctors UUID[], specialties, next-auth). Vaidyasala is a Malayalam health VIDEO discovery platform. Verified against the schema: zero doctor/consultation/booking/price/specialty/availability entities; User.id is String/cuid so the proposed `user_id UUID REFERENCES users(id)` cannot compile (wrong type, and the table is "User" not "users"); prisma schema is a folder not schema.prisma; auth is better-auth not next-auth. Implementing it would have meant inventing an entire marketplace domain inside a video site — dead code, not a feature. Not built; escalated instead.
- [2026-08-10] Delivered the product-agnostic subset of 2E that DOES translate: recent searches in localStorage (max 5, case/whitespace-insensitive de-dup so retyping reorders rather than duplicates, ≥2 chars, survives corrupt storage) + "Clear recent searches", surfaced in the ⌘K palette alongside popular searches when the box is empty. Recorded on select, not per keystroke — history should reflect what led somewhere. Kept off the server deliberately: search history is per-device, most visitors are anonymous, and what someone types into a health search box is better not stored server-side. The 2E items already shipped in 2D (URL-persisted filters, 5-way sort, debounced autocomplete, zero-result alternatives, full-screen mobile palette) were not rebuilt.
- [2026-08-10] CRITICAL bug found by ingesting one real video (08MHzZ2wWhg, "How to remove Stretch Marks", 724 views): /watch/[slug] returned 404 for it. Root cause — slugifyMl deliberately preserves Malayalam, and Next 16 does not round-trip a non-ASCII dynamic route segment: the page IS prerendered but baked with `"status": 404` in its .meta (the cache tag shows the percent-encoded path). Confirmed on the LINUX Docker image, so not a Windows path quirk. Every one of the channel's 503 Malayalam-titled videos would have 404'd on its watch page while still appearing on /latest, in search and in sitemaps — the worst shape of failure, invisible until a user clicks. Fixed with slugifyAscii() for ROUTE slugs (ingest, article, single-video script); slugifyMl is unchanged and still correct for non-URL uses (admin-typed topic slugs). Verified on Linux after the fix: watch 200, no 404 in .meta, real title/duration/stats render, /latest and search both link to the ASCII slug. Seed slugs were all ASCII, which is exactly why 7 phases of testing never caught this.
- [2026-08-10] apps/worker/scripts/add-single-video.ts kept rather than deleted after the proof. It reuses the project's own slug + search-doc builders so it writes rows identical in shape to the real ingest job, and it is the natural tool for spot-checking a video and for staging the throttled backfill. It does NOT run the §8.2 AI chain — no transcript, chapters or enrichment — so it is a metadata/display probe, not a replacement for the pipeline.
- [2026-08-10] Backfill written (scripts/backfill-channel.ts + src/youtube/import.ts, shared with add-single-video). Pages the uploads playlist (UC→UU, 50/page), batches videos.list at the API's 50 ceiling, upserts, indexes to Meili. ~22 quota units for 503 videos. Flags: --limit, --dry-run, --publish, --delay. Defaults to INGESTING, NOT published: importing 503 unreviewed videos straight onto a live site is not a default anyone should get by accident — publishing is opt-in via --publish. Idempotent and resumable: existing rows are refreshed in place and keep their slug and status, so a published URL never moves and a re-run cannot un-publish anything. A failed video is logged and skipped rather than abandoning the remaining batch. METADATA ONLY — the §8.2 AI chain (and its cost) stays with the pipeline.
- [2026-08-10] Verified on real channel data, not fixtures: dry-run 5 → real Malayalam titles; 25 imported (created=25); immediate re-run (created=0 updated=25) proving idempotency; 0 of 30 published slugs contain non-ASCII; Linux image rebuilt → 31 prerendered watch pages with ZERO baked 404s (the pre-fix failure mode), all 24 /latest links 200, search finds an imported video by title, /trending 200.
- [2026-08-10] Real bug in the backfill, surfaced by a task that assumed a two-pass "import then --publish" flow: the upsert's update branch never touched status, so running --publish over already-imported rows was a SILENT NO-OP — it reported updated=N and published nothing. Fixed via promotionFor(), a pure function so it is testable without a database: promotes INGESTING → PUBLISHED only. HIDDEN is an editor's deliberate decision, PROCESSING is mid-pipeline, and PUBLISHED is skipped because re-publishing would reset publishedAt and reshuffle /latest on every run. Nothing demotes. 9 unit tests cover every status plus the null (new row) case. Runtime verification against a DB was NOT possible — the local Docker daemon dropped mid-task — so the logic is proven by unit test only; the DB round-trip still needs one run on a live stack.
- [2026-08-10] Rejected the proposed Dockerfile.worker rewrite: node:20-alpine (project is node:22, and musl breaks Prisma's engines), `pnpm run build` + `CMD node dist/main.js` — which is exactly the layout proven broken earlier, since @vaidyasala/core and @vaidyasala/db export raw .ts and cannot be resolved from tsc output. The current Dockerfile.worker already builds and boots ("ready · workers=3 · pipelineStages=10"); the "worker crashing" premise is stale. Also rejected the `docker compose exec worker pnpm ... tsx scripts/...` invocations: the runtime image deliberately contains only node_modules, dist-bundle and the prisma schema — no pnpm, no tsx, no scripts/. Backfill runs from a repo checkout, or should become a BullMQ job triggerable from /admin/queue.
- [2026-08-10] The pushed BullMQ backfill (496b30a) did not compile — 15 TypeScript errors, so the "TypeScript strict: 0 errors" rule was already broken and `next build` would have failed the web image. Beyond types it had four behavioural faults that would have damaged production: (1) publish mode ran an UNSCOPED `updateMany({status:INGESTING} → PUBLISHED)` with no publishedAt, so every unreviewed video anywhere in the DB goes live at once and /latest orders by null; (2) slug generation stripped Malayalam via `[^\w\s-]` and did NOT append the youtubeId, so an all-Malayalam title produced an EMPTY slug and the next one "-1" — the same class of bug as 681a1a3, plus a check-then-insert race; (3) it wrote non-existent Prisma fields (descriptionMl, thumbnailUrl, duration, viewCount, source) while omitting required ones (durationSec, ytPublishedAt, thumbnails, stats), so create() fails at runtime; (4) a locally-defined promotionFor() was dead code. Rewritten on ../youtube/import, the unit-tested module the CLI script already uses, so there is one import path rather than two that drift.
- [2026-08-10] The admin backfill route had NO AUTH: an unauthenticated POST could import the whole catalogue or publish every video, with the queue name and body shape both public in the repo. Now authorize("ADMIN") on GET and POST, Zod-validated body, 409 when a pass is already queued or running, 202 on accept. Its cross-package `@vaidyasala/worker/...` import (which breaks the web build — separate deployables, no dependency edge) was replaced by the job contract living in @vaidyasala/core/queue, which both sides legitimately depend on. Also fixed BullMQ v5 API misuse in both files: job.progress() → job.updateProgress()/job.progress, job.attempts → job.attemptsMade, un-awaited job.getState() (which serialised every job as {}), and defaultJobOptions passed to Worker instead of Queue.
- [2026-08-10] Two of the three deploy blockers resolved from the real audit data. WEB_PORT = 8888: parsed all 51 LISTEN ports out of the audit and confirmed 8888 is free (nearest neighbours 8180 and 9002), which also happens to be the port every existing runbook and the committed nginx vhost already assume — so nothing else had to change. EXISTING_PROXY = nginx, host-level (owns :80/:443, no proxy container), so integration is one vhost + `nginx -t && reload`. RAM budget ≤3.5GB (40% of 8.8GB available); compose.prod.yml's defaults sum to 3.31GB and fit. SERVER-AUDIT.md rewritten from the raw UTF-16 command dump into the structured document Phase 7 actually gates on, verdict GO. Stripped the UTF-8 BOM from both nginx vhost files — nginx fails to parse a BOM before `upstream`, and a failed `nginx -t` blocks the reload for all 17 sites.
- [2026-08-10] Third blocker (path) made safe rather than guessed: deploy.yml now reads DEPLOY_DIR (repo variable, default /opt/vaidyasala) and aborts with an explicit message if the directory does not exist, instead of `cd`-ing into a path that may be wrong. Which directory is real still needs confirming on the server — the repo has used /opt/vaidyasala, /opt/vaidhyasala and /opt/lsn in different documents.
- [2026-08-10] Flagged as the top deployment risk, not resolved: disk is 91% full (18GB free) on a box hosting 17 projects. Our images are ~1.3GB before volumes, and Postgres + Meilisearch grow with 503 videos of transcripts and embeddings. Reclaim space (dangling images only — never `docker system prune`) and watch `df -h` during the backfill; a full disk breaks all 17 projects simultaneously.

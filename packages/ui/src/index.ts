/**
 * Design system (§4–5): shadcn/ui base + tokens. Tier-1/2 components and the
 * @theme token set land in Phase 1C. This entry exposes the shared class helper
 * every component composes with.
 */
export { cn } from "./lib/cn";
